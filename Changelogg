#!/usr/bin/php
<?php

exec('git pull origin ');
exec('git log --pretty=format:"%h | %an | %cd | %s" --date=short', $buffer);

define('GIT_TITLE', 3);
define('GIT_DATE', 2);
define('GIT_AUTHOR', 1);
define('GIT_HASH', 0);
define('MAX_LOG', 100);

$date = null;
$content = 'ChangeLog';
for ($i = 0; $i<= MAX_LOG; $i++) {
    $commit = $buffer[$i];
